# Python Console Calculator

A simple calculator built in Python for basic operations:

- Addition
- Subtraction
- Multiplication
- Division

The program runs in the terminal and lets the user perform multiple calculations until they choose to exit.

## How to run

1. Install Python.
2. Download `calculator.py`.
3. Run:

```bash
python calculator.py
